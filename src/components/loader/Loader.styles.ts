import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  loadingText: {
    fontSize: 20,
    fontWeight: 'bold',
    padding: 10,
    color: '#2e6fa4',
    textAlign: 'center',
  },
});
