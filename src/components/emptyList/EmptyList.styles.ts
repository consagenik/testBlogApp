import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  emptyListTitle: {
    fontSize: 20,
    fontWeight: 'bold',
    padding: 10,
    color: '#999',
    textAlign: 'center',
  },
});
